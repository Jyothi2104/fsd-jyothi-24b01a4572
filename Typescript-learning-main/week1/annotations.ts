//display my name  with annotations
let lastname: string = "Jyothi";
console.log(lastname);
//function parameter and return type with annotations
function display2(name: string, marks: number): string {
   return `${name} scored ${marks} marks.`;
}
display2("Honey", 95);


//display year array with annotations
let years:number[]=[2026,2027,2028,2029,2030];
console.log(`${years}`)


//display pass with annotations
let valid:boolean=true
console.log(`${valid}`)